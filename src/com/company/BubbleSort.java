package com.company;

// Bubble Sort :- We mostly don't use bubble sort, only in exception cases.
//      Runtime complexity - O(n^2)
//      Space complexity - O(1)
// It's not a practical approach.
// It's not an In-place algorithm. [Space complexity will always be O(1)]
// It's a comparison based sorting algorithm.
/*
    Ex :-
    Take any 2 element and compare whether they are at right position on not.
    Algorithm :-
    for(int i = 0; i < arr.length; i++) {
    for(int j = 1; j < arr.length - 1; j++) {
    if(arr[j-1] > arr[j]) {
    swap(j, j-1);
    }
    }
    }
*/
public class BubbleSort {
    public static void main(String[] args) {

    }
}
