package com.company;

// 1. Partition function :-
// 2. Find pivot element, any element can be pivot element but mostly prefer to pick last element as pivot element.
// 3. All the values which are less than the pivot element lies in left side of the array
//    and element which are more than the pivot element lies in right side of the pivot.
// 4. Swap the values of the pivot with element if it's less than the pivot and s

// 5. worst case :- when data is sorted.

public class QuickSort {
    public static void main(String[] args) {

    }
}
